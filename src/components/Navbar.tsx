import React, {FC} from 'react';
import {Layout, Row} from 'antd';

const Navbar: FC = () => {
    return (
        <Layout.Header>
            <Row justify='end'>

            </Row>
        </Layout.Header>
    );
};

export default Navbar;
